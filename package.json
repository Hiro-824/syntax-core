{
    "name": "syntax-core",
    "version": "0.1.0",
    "description": "Core TypeScript library for parsing and grammar-related logic.",
    "private": true,
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "dev": "tsx src/demo.ts",
        "build": "tsc",
        "check": "tsc --noEmit",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "rimraf": "^6.0.1",
        "tsx": "^4.20.5",
        "typescript": "^5.8.2"
    }
}